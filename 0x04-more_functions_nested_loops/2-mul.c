#include "main.h"

/**
 * mul - multiplies two numbers.
 * @a: variable stores a number to be multiplied.
 * @b: variable stores a number to be multiplied.
 * Return: returns a*b;
 */

int mul(int a, int b)
{
	return (a * b);
}
